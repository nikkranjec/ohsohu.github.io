<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ohsohu</title>

  <style>
    /* 1) FONT */
    @font-face {
      font-family: "Actay";
      src: url("fonts/Actay-Regular.woff2") format("woff2");
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    :root{
      --text: rgba(255,255,255,0.85);
      --text-soft: rgba(255,255,255,0.65);
      --shadow: 0 10px 40px rgba(0,0,0,0.35);
    }

    /* 2) PAGE RESET */
    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    body {
      font-family: "Actay", system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      overflow: hidden;
      background: #000;
    }

    /* 3) FULLSCREEN BACKGROUND */
    .bg {
      position: fixed;
      inset: 0;
      background-image: url("background.jpg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      transform: scale(1.02); /* tiny scale helps avoid edge gaps on some browsers */
    }

    /* Optional: subtle dim / contrast layer for readability */
    .bg::after{
      content:"";
      position:absolute;
      inset:0;
      background: radial-gradient(circle at center, rgba(0,0,0,0.10), rgba(0,0,0,0.35));
      pointer-events:none;
    }

    /* 4) LAYOUT */
    .wrap {
      position: relative;
      height: 100%;
      width: 100%;
      display: grid;
      place-items: center;
    }

    /* 5) TEXT ELEMENTS (3) */
    .top-text {
      position: absolute;
      top: 28px;
      left: 50%;
      transform: translateX(-50%);
      color: var(--text-soft);
      font-size: 14px;
      letter-spacing: 0.02em;
      user-select: none;
      white-space: nowrap;
    }

    .bottom-text {
      position: absolute;
      bottom: 28px;
      left: 50%;
      transform: translateX(-50%);
      color: var(--text-soft);
      font-size: 14px;
      letter-spacing: 0.02em;
      user-select: none;
      white-space: nowrap;
    }

    /* third text element lives in the center area */
    .center-title {
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      color: var(--text);
      font-size: clamp(28px, 3.2vw, 44px);
      letter-spacing: 0.01em;
      user-select: none;
      pointer-events: none;
    }

    /* 6) SQUARE VIDEO SLOT */
    .video-slot {
      width: min(520px, 62vw);
      aspect-ratio: 1 / 1;
      position: relative;
      border-radius: 0px; /* keep it square */
      overflow: hidden;
      box-shadow: var(--shadow);
      background: rgba(0,0,0,0.35);
      backdrop-filter: blur(2px);
    }

    /* If you want a subtle inner ring / glow like your mock */
    .video-slot::before{
      content:"";
      position:absolute;
      inset: 10%;
      border-radius: 999px;
      border: 2px solid rgba(255,255,255,0.08);
      filter: blur(0.2px);
      pointer-events:none;
    }

    .video-slot video,
    .video-slot iframe {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      border: 0;
      display: block;
    }

    /* Placeholder if no video yet */
    .placeholder {
      position: absolute;
      inset: 0;
      display: grid;
      place-items: center;
      color: rgba(255,255,255,0.45);
      font-size: 13px;
      letter-spacing: 0.02em;
      user-select: none;
    }
  </style>
</head>

<body>
  <div class="bg" aria-hidden="true"></div>

  <main class="wrap">
    <!-- TEXT 1 -->
    <div class="top-text">oh so human</div>

    <!-- CENTER VIDEO SLOT -->
    <div class="video-slot">
      <!-- Replace placeholder with your video when ready:
           <video src="your-video.mp4" autoplay muted loop playsinline></video>
        -->
      <div class="placeholder">square video goes here</div>
    </div>

    <!-- TEXT 2 (center) -->
    <div class="center-title">oh so hu</div>

    <!-- TEXT 3 -->
    <div class="bottom-text">by people who love.</div>
  </main>
</body>
</html>
